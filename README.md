# IDS-Intrusion-Detection-System-
Capture du trafic réseau (pcap), analyse de signatures simples (ex. scans de ports tentatives de login SSH), génération d’alertes.

### [Inspiration]((https://www.freecodecamp.org/news/build-a-real-time-intrusion-detection-system-with-python/))

promiscuous mode (permet que tout le traffic passe dans le CPU plutôt que seulement les packets traitable par le CPU)



Isolation forest is an unsupervide learning (learn patterns exclusively from unlabeled data) algorithm for anomaly detection.